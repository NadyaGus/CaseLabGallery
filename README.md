# Case Lab Gallery

To install project:

Please make sure you have docker running locally.
After that enter the following commands:

```js
npm install
docker build -t cors .
cd directus
docker compose up
```
